import 'package:flutter/cupertino.dart';

class StorefoodPage extends StatelessWidget {
  const StorefoodPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Storefood Page"),
      ),
    );
  }
}
